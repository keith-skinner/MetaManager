<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pocketspace.metamanager">

    <!-- TODO: Make the onclick actually work -->

    <application
        android:icon="@mipmap/hat_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/hat_ic_launcher_round"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity
            android:name=".screen_activities.ActivityHomeScreen"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".screen_activities.ActivityRole"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".screen_activities.ActivityBuildView"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".screen_activities.ActivityAboutScreen"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".screen_activities.ActivityViewBuildsScreen"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".screen_activities.ActivityImportScreen"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".screen_activities.ActivityCreateScreen"
            android:theme="@style/AppTheme"/>

    </application>

</manifest>